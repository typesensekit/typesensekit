{
  "name": "typesensekit",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build && pnpm --filter @typesensekit/web build",
    "dev": "pnpm -r --parallel --filter './packages/*' run dev",
    "dev:web": "pnpm --filter @typesensekit/web dev",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome format --write .",
    "docs:coverage": "pnpm build && node scripts/generate-api-coverage.mjs",
    "docs:check": "node scripts/generate-api-coverage.mjs --check",
    "changeset": "changeset",
    "release": "changeset publish",
    "release:ci": "node scripts/skip-published-packages.mjs && changeset publish",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm docs:check && pnpm --filter @typesensekit/mcp smoke:stdio",
    "pack:dry": "npm pack --dry-run --json ./packages/cli && npm pack --dry-run --json ./packages/mcp",
    "prepublishOnly": "pnpm check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13",
    "@changesets/cli": "^2.31.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  },
  "description": "CLI and MCP server for the Typesense API.",
  "license": "MIT",
  "author": "Akshit Kr Nagpal",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/typesensekit/typesensekit.git"
  },
  "bugs": {
    "url": "https://github.com/typesensekit/typesensekit/issues"
  },
  "homepage": "https://github.com/typesensekit/typesensekit#readme"
}
